# Copy to .env (gitignored). The LLM-as-judge + grounding layers call the Anthropic API.
# Advisory checks run WITHOUT a key (`npm run eval -- --checks-only`); classification is skipped.
ANTHROPIC_API_KEY=sk-ant-your-key-here
